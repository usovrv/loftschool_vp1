{% include('common/_header.twig') %}

<div class="main"> <!-- доп.обёртка для прижатия футера -->

    {% include('common/_nav.twig') %}

    <div class="content">
        <div class="wrapper">
            <main>
                <h1>Выпускной проект №1 - Блог</h1>
                <form action="" method="post" enctype="multipart/form-data">
                    <h2>Все записи</h2>
                    <div class="form-group">
                        <div class="form-subgroup">
                            <label for="inputName">Имя</label>
                            <input type="text" class="form-control" id="inputName" placeholder="Введите Ваше имя" name="name">
                            <input type="hidden" name="userId" value="{{ userId }}">
                        </div>
                        <div class="form-subgroup">
                            <label for="inputName">Текст</label>
                            <textarea name="text" cols="60" rows="10"></textarea>
                        </div>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-submit" name="submit">Отправить</button>
                    </div>
                </form>
            </main>
        </div>
    </div>
</div>

{% include('common/_footer.twig') %}
